/**
 * Ad-Hoc Communication Subsystem
 *
 * Centralized WebSocket message protocol and messenger classes
 * for gateway -> client communication.
 *
 * @module gateway/ad-hoc
 */

export { MessageTypes } from './types.js';
export { ClientMessenger, BroadcastMessenger } from './messenger.js';
